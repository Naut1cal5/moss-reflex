# Recurring failure task

Check out a clean copy of the target fixture, run its test suite, diagnose the recurring import
failure, apply the smallest correct fix, and verify that all tests pass. Count every agent tool
call and record the model's total input plus output tokens through resolution.
